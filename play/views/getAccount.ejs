<%- include header %>

<div class="app-body">
  <%- include menu %>
  <main class="main">
    <!-- Breadcrumb-->
    <ol class="breadcrumb">
      <li class="breadcrumb-item">Basic</li>
      <li class="breadcrumb-item active">Account address</li>
    </ol>

    <div class="container-fluid">
      <div class="animated fadeIn">
        <div class="card">
          <div class="card-header">getAccount()</div>
          <div class="card-body">
            <div class="bd-example">
              Get MetaMask address
          </div>
            </div>
        </div>
        </div>
    </div>

    <%- include code %>

  </main>
</div>
<script type="text/javascript">

  function runCode() {
    watchAccount();
  }

  function watchAccount() {
    var res = getWeb3Account();
    var result = "Account:" + res.data +"\n";
    if(res.code!=0) {
      result += res.msg+ '\n'
    }

    setResult(result);

  }

  // var Web3 = require('web3');
  $(document).ready(function() {
    var code = runCode.toString()+"\n\n";
    code += getWeb3Account.toString()+"\n\n";
    code += watchAccount.toString() + "\n\n";
    code += "runCode();\n\n";
    setCode(code);

    watchAccount();

  });

</script>
<%- include footer %>
